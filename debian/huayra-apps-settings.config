#!/bin/sh

set -e

. /usr/share/debconf/confmodule

db_version 2.0

db_input medium huayra-apps-settings/autologin || true
db_go

db_get huayra-apps-settings/autologin

if [ "$RET" = "true" ]; then
  db_input medium huayra-apps-settings/username || true
  db_go
fi

