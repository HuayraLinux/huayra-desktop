#!/bin/sh -e

set -e

if [ "$1" = "purge" ]; then
    if dpkg-statoverride --list --quiet "/var/www" >/dev/null;
    then
	dpkg-statoverride --force --quiet --remove "/var/www"
    fi
fi



#DEBHELPER#
